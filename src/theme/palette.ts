export const palette = {
  white: "#FFFFFF",
  main: "#8E97FD",
  lightMain: "#8C96FF",
  darkGray: "#3F414E",
  lightGray: "#A1A4B2",
  grey: "#F2F3F7",
  lightGrey: "#EBEAEC",
  lightYellow: "#FFECCC",
};

// const [data, setData] = useState<Courses[]>();

// const getCourses = async (topic: string) => {
//   try {
//     const response = await axios.get<Courses[]>(`https://api.deezer.com/search/podcast?q=${topic}&limit=10`)
//     setData(response.data);
//   } catch (error) {
//     console.error(error);
//   }
// };

// useEffect(() => {
//   getCourses();
// }, []);

// console.log(data);